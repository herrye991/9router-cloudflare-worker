{
  "name": "9router-cloudflare-worker",
  "version": "0.1.0",
  "description": "9Router Cloudflare Workers implementation — isolated, Hono + D1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "test": "npx tsx --test tests/worker.test.ts"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.99.0"
  }
}
